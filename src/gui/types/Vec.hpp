#pragma once

#include <glm/glm.hpp>

namespace Zappy
{

/// @brief 2D float vector (alias over GLM, kept as a swap seam).
using Vec2 = glm::vec2;

/// @brief 3D float vector (alias over GLM, kept as a swap seam).
using Vec3 = glm::vec3;

/// @brief 4D float vector (alias over GLM, kept as a swap seam).
using Vec4 = glm::vec4;

} // namespace Zappy
